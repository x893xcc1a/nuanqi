陪聊V3.0 产品需求与系统流转文档
一、项目概述
1.1 产品背景
陪聊V3.0 是一款基于微信公众号+H5的陪聊服务平台，用户可在线选择陪聊店员（语音/视频/文字），通过微信支付下单，系统自动匹配或选择指定店员，完成服务后进行评价。

1.2 核心功能
用户端：首页推荐、盲盒匹配、选人下单、动态广场、个人中心、客服中心
店员端：微信公众号内极简接单页、订单管理、收益查看
管理端：订单审核、用户管理、店员管理、数据统计
二、用户端H5页面流转图
2.1 整体导航结构
Text
📱 公众号菜单
├── 首页（H5主入口）
├── 动态广场
├── 我的订单
├── 个人中心
└── 成为店员（独立申请页）
2.2 首页（底部5个Tab导航）
Text
┌─────────────────────────────────┐
│           首页                   │
├─────────────────────────────────┤
│  [心动墙]  [盲盒]  [选人下单]  [动态]  [个人中心] │
├─────────────────────────────────┤
│                                 │
│  ┌───── 心动墙 ──────┐         │
│  │  · 打赏动态流      │         │
│  │  · 男神榜/女神榜   │         │
│  │  · 随机盲盒入口    │         │
│  │  · 福利中心入口    │         │
│  │  · 钱包快捷入口    │         │
│  └─────────────────────┘         │
│                                 │
│  ┌───── 盲盒 ────────┐         │
│  │  · 随机匹配店员    │         │
│  │  · 选择服务类型    │         │
│  │  · 选择时长        │         │
│  │  └→ 下单页         │         │
│  └─────────────────────┘         │
│                                 │
│  ┌───── 选人下单 ────┐         │
│  │  · 搜索/筛选店员   │         │
│  │  · 店员列表        │         │
│  │  · 店员详情页      │         │
│  │  └→ 下单页         │         │
│  └─────────────────────┘         │
│                                 │
│  ┌───── 动态广场 ────┐         │
│  │  · 用户动态        │         │
│  │  · 店员动态        │         │
│  │  · 发布动态(用户)  │         │
│  │  · 点赞/评论       │         │
│  └─────────────────────┘         │
│                                 │
│  ┌───── 个人中心 ────┐         │
│  │  · 用户头像/昵称   │         │
│  │  · 我的订单        │         │
│  │  · 我的钱包        │         │
│  │  · 分销中心        │         │
│  │  · 客服中心        │         │
│  │  · 礼物图鉴        │         │
│  │  · 收藏店员        │         │
│  │  · 设置            │         │
│  └─────────────────────┘         │
└─────────────────────────────────┘
2.3 订单核心流程（用户视角）
Text
首页 → 选人下单/盲盒 → 店员详情页 → 下单页 → 填写微信号+需求 → 支付页面 → 订单详情页（待接单）
待接单 → 店员接单 → 订单详情页（服务中）→ 显示店员微信号 → 用户添加微信
服务中 → 店员标记完成 → 订单详情页（待确认）→ 用户确认/超时自动确认 → 订单详情页（已完成）→ 评价弹窗 → 完成
2.4 盲盒下单流程
Text
盲盒页面 → 选择服务类型 → 选择时长 → 填写微信号 → 支付 → 订单详情 → 等待店员接单 → 获得店员微信号 → 添加到微信
2.5 选人下单流程
Text
店员列表 → 筛选条件（分类/价格/性别/在线状态） → 店员卡片 → 店员详情页（头像/昵称/价格/语音简介/照片墙/评价） → 立即下单按钮 → 下单页 → 选择服务时长 → 填写微信号 → 支付 → 订单详情
2.6 动态广场页面流转
Text
动态广场 → 动态列表（用户动态/店员动态）
动态广场 → 发布按钮 → 发布动态页（文字输入/图片上传/可见范围/提交审核）
动态列表 → 点击单条动态 → 动态详情页 → 点赞/评论/打赏礼物
打赏礼物 → 选择礼物 → 支付 → 打赏成功
2.7 客服中心页面流转
Text
客服中心 → 常见问题列表 → 问题分类 → 具体问题详情
客服中心 → 在线客服 → 提交工单（选择问题类型/描述/截图） → 工单列表 → 工单详情（客服回复/关闭工单）
微信公众号 → 模板消息（客服回复） → 点击进入工单详情
2.8 个人中心子页面流转
Text
个人中心 → 我的订单 → 全部订单列表 → 订单详情页
个人中心 → 我的钱包 → 余额/充值/消费记录
个人中心 → 分销中心 → 推广二维码/收益明细
个人中心 → 客服中心 → 常见问题/提交工单
个人中心 → 礼物图鉴 → 已收集礼物
个人中心 → 收藏店员 → 店员列表 → 店员详情页
个人中心 → 设置 → 个人信息/退出登录
2.9 订单详情页状态流转
状态	说明	操作按钮
待付款	订单已创建，等待用户支付	去支付/取消订单
待接单	已支付成功，等待店员接单	取消订单(退款)
服务中	店员已接单，显示店员微信号	联系客服
待确认	店员标记完成，等待用户确认	确认完成/联系客服
已完成	用户已确认或超时自动确认	评价/再次下单
2.10 兜底与异常流程
异常类型	处理方式
店员24小时未接单	系统自动取消，全额退款至钱包，推送通知
用户申请退款	联系客服，后台人工审核，同意或拒绝退款
用户未添加微信	系统重复提醒（短信+模板消息）
店员联系不上	平台介入，更换店员
网络异常	显示重试按钮/返回首页/离线缓存
三、店员接单极简页（微信公众号内）
3.1 店员消息通知点击流程
Text
微信公众号 → 模板消息通知 → 点击通知 → 微信OAuth授权 → 订单通知卡片页
订单通知卡片页 → 展示订单信息（服务类型+时长+用户微信号+用户备注）
订单通知卡片页 → 复制微信号 → 确认接单 → 订单进行中页（服务倒计时/用户微信号/标记服务完成）
订单通知卡片页 → 拒绝接单 → 订单回池
3.2 订单通知卡片页
Text
┌─────────────────────────────────┐
│         新订单通知               │
├─────────────────────────────────┤
│ 📋 订单信息                     │
│ ├─ 服务类型: 语音聊天           │
│ ├─ 服务时长: 30分钟             │
│ ├─ 服务价格: ¥50                │
│ └─ 用户备注: 心情不好需要安慰   │
│                                 │
│ 📱 用户微信号                   │
│ ┌─────────────────────┐        │
│ │ wechat_user_123     │ [复制]  │
│ └─────────────────────┘        │
│                                 │
│ 温馨提示: 请复制微信号添加用户   │
│                                 │
│ ┌──────────┐  ┌──────────┐    │
│ │ 拒绝接单  │  │ 确认接单  │    │
│ └──────────┘  └──────────┘    │
└─────────────────────────────────┘
3.3 订单进行中页
Text
┌─────────────────────────────────┐
│         服务进行中               │
├─────────────────────────────────┤
│ ⏱️ 服务倒计时                   │
│  ┌─────────────────────────┐   │
│  │     25:30               │   │
│  └─────────────────────────┘   │
│                                 │
│ 📱 服务对象微信号               │
│ ┌─────────────────────┐        │
│ │ wechat_user_123     │ [复制]  │
│ └─────────────────────┘        │
│                                 │
│ 📝 温馨提示:                    │
│ 在微信中完成服务后，点击下方按钮 │
│                                 │
│ ┌─────────────────────────┐    │
│ │     标记服务完成          │    │
│ └─────────────────────────┘    │
└─────────────────────────────────┘
四、后端流转图
4.1 整体架构图
Text
客户端: H5前端 + 微信公众号
       ↓
网关层: Nginx/反向代理 → API网关
       ↓
应用服务层: 用户服务 | 订单服务 | 店员服务 | 支付服务 | 动态服务 | 客服服务 | 分销服务 | 消息推送服务
       ↓
数据层: MySQL(主从) + Redis(缓存) + 消息队列 + 对象存储 + ES搜索引擎
       ↓
外部依赖: 微信支付 + 微信开放平台 + 短信服务商
4.2 用户下单完整流程（时序图）
Text
用户H5 → 订单服务: 创建订单(店员ID/盲盒)
订单服务 → 订单服务: 校验店员在线状态
订单服务 → 订单服务: 生成订单(prepare状态)
订单服务 → 订单服务: 锁定店员(Redis锁)
订单服务 → 用户H5: 返回订单ID
用户H5 → 支付服务: 发起支付
支付服务 → 支付服务: 创建微信支付订单
支付服务 → 微信支付: 统一下单API
微信支付 → 支付服务: 返回prepay_id
支付服务 → 用户H5: 返回支付参数
用户H5 → 微信支付: 调起微信支付
微信支付 → 用户H5: 支付结果回调
用户H5 → 支付服务: 支付成功通知
支付服务 → 订单服务: 更新订单状态(paid→pending)
订单服务 → 订单服务: 计算自动取消时间
订单服务 → 消息服务: 发送模板消息给店员
消息服务 → 店员: 微信模板消息通知
店员 → 订单服务: 确认接单
订单服务 → 订单服务: 更新订单状态(serving)
订单服务 → 订单服务: 启动服务倒计时
店员 → 订单服务: 标记服务完成
订单服务 → 订单服务: 更新订单状态(completed)
订单服务 → 订单服务: 启动自动确认倒计时
用户H5 → 订单服务: 确认完成/评价
订单服务 → 订单服务: 更新订单状态(done)
订单服务 → 订单服务: 店员结算
4.3 服务间调用关系
创建订单流程
Text
用户请求创建订单 → 判断订单类型(选人/盲盒)
选人: 校验店员ID → 校验店员状态
盲盒: 匹配店员 → 校验店员状态
↓
店员是否在线? → 是: 锁定店员资源 → 生成订单号 → 保存订单到MySQL → 存入Redis订单缓存 → 返回订单ID
              → 否: 返回错误
支付回调处理
Text
微信支付回调 → 验签 → 查询订单 → 更新订单状态为paid → 记录支付流水 → 释放Redis锁定 → 发送消息到MQ
订单超时处理
Text
定时任务(每10秒) → 扫描即将超时的服务 → 发送服务提醒通知
定时任务(每分钟) → 扫描待付款订单(超时30分钟) → 取消订单 → 释放店员
定时任务(每分钟) → 扫描等待接单订单(超时24小时) → 自动取消 → 全额退款
定时任务(每分钟) → 扫描待确认订单(超时24小时) → 自动确认 → 结算店员收益
定时任务(每小时) → 同步店员在线状态/清理过期缓存/生成运营统计数据
定时任务(每天凌晨) → 店员结算汇总/数据对账/发送日报
4.4 数据流转图
订单数据流转
Text
用户下单 → API请求 → 订单服务 → 写入MySQL orders表 → 写入Redis缓存 → 写入Redis店员锁定标记 → 发送MQ: order.created
支付成功 → 微信回调 → 支付服务 → 更新MySQL orders表status=paid → 更新Redis缓存 → 发送MQ: order.paid
店员接单 → 店员确认 → 店员服务 → 更新MySQL orders表status=serving → 更新Redis缓存 → 写入Redis计时器 → 发送MQ: order.accepted
Redis缓存设计
Text
order:{order_id}: hash类型 - 存储订单信息
clerk_online:{clerk_id}: string - 店员在线状态(1/0)
clerk_locked:{clerk_id}: string - 店员锁定(存储订单ID,TTL 600秒)
service_timer:{order_id}: string - 服务计时器(动态TTL)
order_pay_lock:{order_id}: string - 支付防重锁(TTL 60秒, NX模式)
4.5 消息队列流转
MQ Topics设计
Text
order.created         - 订单创建
order.paid            - 支付成功
order.accepted        - 店员接单
order.completed       - 服务完成
order.cancelled       - 订单取消
order.refunded        - 订单退款
user.register         - 用户注册
clerk.register        - 店员注册
notification.send     - 消息推送
evaluation.created    - 评价完成
4.6 数据库表流转关系
Text
users (用户表) → orders (订单表) → evaluations (评价表)
clerks (店员表) → orders (订单表) → payments (支付流水表)
4.7 异常处理流程
支付异常
Text
支付接口超时 → 重试3次 → 成功则正常处理，失败则返回给用户"支付失败"
订单状态不一致 → 查询微信支付状态 → 已支付则修复本地订单状态，未支付则保持原状
退款失败 → 记录失败原因 → 发送告警 → 人工介入处理
订单对账
Text
定时任务查询特定时间内订单 → 按状态分组处理
已付款但未接单超过24小时 → 自动取消 → 发起退款 → 记录日志
已接单但超时未完成 → 标记异常 → 记录日志 → 发送告警
4.8 安全与权限
权限认证流程
Text
用户请求授权 → 微信回调code → 换取access_token → 获取用户信息 → 生成JWT Token → 返回Token
用户携带Token请求API → 验证Token签名 → 从Token解析用户信息 → 返回业务数据
接口防刷策略
Text
请求到达 → IP限流(超出返回429) → 用户限流(超出返回429) → Token验证(失效返回401) → 签名校验(错误返回403) → 执行业务逻辑
4.9 API接口汇总
Text
用户端:
  POST /api/users/register      - 用户注册
  GET  /api/users/info          - 获取用户信息
  PUT  /api/users/info          - 更新用户信息
店员端:
  POST /api/clerks/register     - 店员注册
  GET  /api/clerks/list         - 店员列表
  GET  /api/clerks/:id          - 店员详情
  PUT  /api/clerks/status       - 更新在线状态
订单端:
  POST /api/orders              - 创建订单
  GET  /api/orders/:id          - 订单详情
  GET  /api/orders              - 订单列表
  POST /api/orders/cancel       - 取消订单
  POST /api/orders/accept       - 店员接单
  POST /api/orders/complete     - 店员标记完成
  POST /api/orders/confirm      - 用户确认完成
支付端:
  POST /api/payments/create     - 创建支付
  POST /api/payments/notify     - 支付回调
  POST /api/payments/refund     - 发起退款
动态端:
  POST /api/feeds               - 发布动态
  GET  /api/feeds               - 动态列表
  POST /api/feeds/like          - 点赞动态
  POST /api/feeds/comment       - 评论动态
  POST /api/feeds/gift          - 打赏礼物
客服端:
  POST /api/tickets             - 提交工单
  GET  /api/tickets             - 工单列表
  GET  /api/tickets/:id         - 工单详情
五、Mermaid图表（兼容Markdown渲染）
5.1 订单核心流程
是

否-超时

是

否

是

超时自动确认

首页

选人下单/盲盒

店员详情页

下单页

填写微信号+需求

支付页面

订单详情页-待接单

店员接单?

订单详情页-服务中
显示店员微信号

订单关闭/退款

店员标记完成?

订单详情页-待确认

用户确认?

订单详情页-已完成

评价弹窗

评价完成-返回首页

5.2 用户下单完整流程
店员
微信支付
消息服务
店员服务
支付服务
订单服务
用户H5
店员
微信支付
消息服务
店员服务
支付服务
订单服务
用户H5
1. 创建订单(店员ID/盲盒)
2. 校验店员在线状态
3. 生成订单(prepare状态)
4. 锁定店员(Redis锁)
5. 返回订单ID
6. 发起支付
7. 创建微信支付订单
8. 统一下单API
9. 返回prepay_id
10. 返回支付参数
11. 调起微信支付
12. 支付结果回调
13. 支付成功通知
14. 更新订单状态(paid->pending)
15. 计算自动取消时间
16. 发送模板消息给店员
17. 微信模板消息通知
18. 确认接单
19. 更新订单状态(serving)
20. 启动服务倒计时
21. 标记服务完成
22. 更新订单状态(completed)
23. 启动自动确认倒计时
24. 确认完成/评价
25. 更新订单状态(done)
26. 店员结算
5.3 服务间调用关系
支付回调处理

微信支付回调

验签

查询订单

更新订单状态为paid

记录支付流水

释放Redis锁定

发送消息到MQ

创建订单流程

选人下单

盲盒下单

是

否

用户请求创建订单

订单类型

校验店员ID

匹配店员

校验店员状态

店员是否在线

锁定店员资源

返回错误

生成订单号

保存订单到MySQL

存入Redis订单缓存

返回订单ID

5.4 定时任务
日级任务

每天凌晨

店员结算汇总

数据对账

发送日报

清理日志

小时级任务

每小时

同步店员在线状态

清理过期缓存

生成运营统计数据

分钟级任务

每分钟

扫描待付款订单

取消超30分钟未支付的订单

每分钟

扫描等待接单订单

自动取消超24小时未接单

触发全额退款流程

每分钟

扫描待确认订单

自动确认超24小时未确认

结算店员收益

秒级任务

每10秒

扫描即将超时的服务

发送服务提醒通知

5.5 数据库表关系
Parse error on line 3:
... users        U1[id(PK)]        U2[ope
----------------------^
Expecting 'SQE', 'DOUBLECIRCLEEND', 'PE', '-)', 'STADIUMEND', 'SUBROUTINEEND', 'PIPE', 'CYLINDEREND', 'DIAMOND_STOP', 'TAGEND', 'TRAPEND', 'INVTRAPEND', 'UNICODE_TEXT', 'TEXT', 'TAGSTART', got 'PS'
六、版本历史
版本	日期	修改内容	修改人
V1.0	2024-03-xx	初始文档创建	-
说明：本文档包含流程图（Mermaid格式）和文字描述，支持Markdown渲染器（如GitHub、Typora、Obsidian、VS Code等）直接预览图表。
